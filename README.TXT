test test


#27 yes it shows the same exact pattern

#28 its used to reset the automaton by filling the state with zeros

#29 the pattern changes compared too the original one but if you run it again you get the same pattern each time

#31 we use the nextState because if we change the state array while we are still using it to calculate new valeus we will lose the original value

#32 Yes, because we only need to remember the old value of two cells at a time. we can store just a few temporary variables instead of a full second array

#35 the next state of a cell depends on left, center, and right, each 0 or 1. by combining them in diffrent ways (mod 2), we can create diffrent rules. there are 8 input so 2⁸ = 256.

#36

#36